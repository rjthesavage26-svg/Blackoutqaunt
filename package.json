{
  "name": "blackout-quant",
  "private": true,
  "version": "0.1.0",
  "description": "AI-powered TradingView paper trading workstation for learning.",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "preview": "npm --prefix frontend run preview"
  },
  "engines": {
    "node": ">=20"
  }
}
